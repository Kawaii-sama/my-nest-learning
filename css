1. selectors and declaration box:-
2. selectors { property : value; }
3. selectors can be tags, classes, ids or their combinations.
4. Anything can be added in that is html?
5. After selectors are done, we need to define styles corresponding styles which get applied on selectors in HTML.